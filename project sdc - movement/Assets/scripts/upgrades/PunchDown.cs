using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PunchDown : MonoBehaviour
{
    Transform player;
    public float energy;
    public bool active;
    public bool HM;

    public float radious;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
